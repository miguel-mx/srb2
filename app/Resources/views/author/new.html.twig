{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}

    {{ form_errors(form) }}
    <section id="new-author">
        <div class="container-fluid myTab" >
            <div class="row justify-content-md-center">
                <div class="col-9" style="background-color: #eeeeee">
                    <div class="row referencia-container p-5">
                        <div class="col-md-12 ">
                            <form>
                                <h1>Cites creation</h1>
{#                                <h3>{{ cite.references.title }}</h3>#}

                                <div class="form-row">
                                    <div class="form-group col-md-6">
                                        <label for="name">{{ form_label(form.name) }}</label>
                                        {{ form_errors(form.name) }}
                                        {{ form_widget(form.name, { 'attr': { 'class': "form-control", 'placeholder': "Name" , 'id': "name" } } ) }}
                                    </div>
                                    <div class="form-group col-md-6">
                                        <label for="user">{{ form_label(form.user) }}</label>
                                        {{ form_errors(form.user) }}
                                        {{ form_widget(form.user, { 'attr': { 'class': "form-control", 'id': "user" } } ) }}
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label for="alias">{{ form_label(form.alias) }}</label>
                                    {{ form_errors(form.alias) }}
                                    {{ form_widget(form.alias, { 'attr': { 'class': "form-control", 'placeholder': "Alias" , 'id': "alias" } } ) }}
                                </div>
                                <div class="form-group">
                                    <label for="referencia">{{ form_label(form.referencia) }}</label>
                                    {{ form_errors(form.referencia) }}
                                    {{ form_widget(form.referencia, { 'attr': { 'class': "form-control", 'id': "referencia"  } } ) }}
                                </div>
                                <div>
                                    {#                                        {{ form_widget(edit_form) }}#}
                                    <button type="submit" class="btn btn-primary">Guardar</button>
                                    <a href="javascript:history.back(-1);" title="Ir la página anterior" class="btn btn-danger">Cancelar <i class="fas fa-times fa-x3"></i></a>
                                    {#                                                                    {{ form_end(form) }}#}
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{#    <h1>Author creation</h1>#}

{#    {{ form_start(form) }}#}
{#        {{ form_widget(form) }}#}
{#        <input type="submit" value="Create" />#}
{#    {{ form_end(form) }}#}

{#    <ul>#}
{#        <li>#}
{#            <a href="{{ path('author_index') }}">Back to the list</a>#}
{#        </li>#}
{#    </ul>#}
{% endblock %}
